# .sqlfluff

# default config: https://docs.sqlfluff.com/en/stable/configuration/default_configuration.html#defaultconfig
# rules ref: https://docs.sqlfluff.com/en/stable/reference/rules.html#ruleref

[sqlfluff]
dialect = tsql
encoding = utf-8
templater = jinja
output_line_length = 120
max_line_length = 120

[sqlfluff:layout:type:comma]
spacing_before = touch
line_position = trailing

[sqlfluff:rules:capitalisation.keywords]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.identifiers] 
extended_capitalisation_policy = pascal 

[sqlfluff:rules:capitalisation.functions] 
extended_capitalisation_policy = snake

[sqlfluff:rules:capitalisation.literals] 
capitalisation_policy = lower

[sqlfluff:rules:capitalisation.types] 
extended_capitalisation_policy = snake

[sqlfluff:rules:layout.long_lines]
ignore_comment_lines = True

[sqlfluff:exclude]
path = migrations/, tests/

# note:
# 1. When a statement is written as ( ... ); — that is, ending with a semicolon (some syntax, such as `merge`, requires a semicolon at the end)
# The two parentheses cannot be on the same line; otherwise, it will cause an error (likely a bug). 
# Therefore, it’s better to make a habit of placing the closing parenthesis on a new line.

# 2. format must the same, ex. select * from [T1], [T2] (not select * from [T1], T2)
